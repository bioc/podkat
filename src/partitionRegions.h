//
// File partitionRegions.h defining prototypes for partitionRegions.cpp
//

#ifndef _partitionRegions_H_

#define _partitionRegions_H_

RcppExport SEXP partitionRegions(SEXP seqNamesR, SEXP startR, SEXP endR,
                                 SEXP widthR, SEXP overlapR);

#endif
